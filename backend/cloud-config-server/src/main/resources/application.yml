spring:
  application:
    name: configuration-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/FilipZobic/online-university-microservices-config-server.git
          username:
          password:
          clone-on-start: true
server:
  port: 8888